export function ShopTrustLine({ className = "" }: { className?: string }) {
  return (
    <p className={`text-[11px] leading-5 tracking-[0.04em] text-mocha/50 ${className}`}>
      COD · Free nationwide delivery · 7-day exchange
    </p>
  );
}
